# Frontend Rules

- Use Expo Router structure.
- Maintain reusable components in `src/components/`.
- Follow dark theme consistency using Expo ThemeProvider.
- Keep subscription/add-on logic intact and centralized.
- Use TypeScript-safe patterns; avoid `any`.
- Prefer composition over inheritance for components.
- Keep styles co-located with components using `StyleSheet.create`.
