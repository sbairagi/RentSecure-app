# Testing Rules

- Add/update tests for major features and reusable components.
- Prefer unit tests for hooks and utilities.
- Avoid breaking CI pipeline.
- Use Jest with React Native Testing Library for component tests.
- Mock native modules and Expo APIs in tests.
- Keep tests deterministic; avoid network or file system dependencies.
