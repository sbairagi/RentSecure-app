import Constants from 'expo-constants';

export const APP_NAME = Constants.expoConfig?.name || 'SecureNest';
export const APP_VERSION = Constants.expoConfig?.version || '1.0.0';
export const APP_SCHEME = Constants.expoConfig?.scheme || 'securenest';
